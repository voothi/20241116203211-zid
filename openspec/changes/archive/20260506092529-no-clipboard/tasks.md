## 1. Implementation

- [x] 1.1 Add `argparse` support to `zid.py`.
- [x] 1.2 Add a `--no-clipboard` flag and update the clipboard copy logic.
- [x] 1.3 Ensure the script still prints the generated ZID in both default and no-clipboard modes.

## 2. Verification

- [x] 2.1 Add or update tests to confirm `python zid.py --no-clipboard` prints a ZID without copying it.
- [x] 2.2 Add or update tests to confirm default `python zid.py` still copies to clipboard.

## 3. Documentation

- [x] 3.1 Document the new `--no-clipboard` option in README or user-facing docs.
